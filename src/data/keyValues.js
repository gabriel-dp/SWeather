// These values are in metric system, conversions are made only to display to the user

const keyValues = {
	MIN_WIND_SPEED: 5,
	MAX_PARTICLES: 25,
	MIN_CLOUDY: 10,
	MAX_CLOUDY: 80,
};

export default keyValues;
